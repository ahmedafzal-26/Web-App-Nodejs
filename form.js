function submitForm() // Submit form data to server
{
    const firstName = document.getElementById('firstName').value; // Get form data
    const lastName = document.getElementById('lastName').value; // Get form data
    const email = document.getElementById('email').value;  // Get form data
    const phoneNumber = document.getElementById('phoneNumber').value;  // Get form data
    const eircode = document.getElementById('eircode').value;  // Get form data 
}